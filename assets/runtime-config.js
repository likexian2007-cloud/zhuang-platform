// 运行时后端地址。这里只能放公开服务 URL，不能放任何 API Key。
window.PLATFORM_API = window.PLATFORM_API || "https://hot-clubs-own.loca.lt";
// 正式私有部署时可由服务端注入短期访问令牌；不要把长期密钥写在前端仓库。
window.PLATFORM_TOKEN = window.PLATFORM_TOKEN || "";
